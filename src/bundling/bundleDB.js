/*
This file will be used to write bundle data to a database

The table containing bundles will contain three tables: data (the stringified body data), moat name, when it was inserted into the database (unix timestamp), the endpoint it was sent to, and an auto incrementer.
*/

const write2Bundle = async (_req, _data) => {
    
}