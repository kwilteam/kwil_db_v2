const signatures = require('./signatures');

describe("checkQuerySig", () => {
    it("Returns true with correctly inputted data object", async () => {
        
    });
    it("Returns false with incorrect data object", async () => {
        
    });
});

describe("createSignedQuery", () => {
    it("Returns correct object with inputted data", () => {
        
    });
});